/**
 * 云端服务器配置示例
 * 
 * 使用方法：
 * 1. 复制此文件为 cloudConfig.ts
 * 2. 修改 API_BASE_URL 为你的服务器地址
 * 3. cloudConfig.ts 不会被提交到 git（已在 .gitignore 中）
 */

export const API_BASE_URL = 'http://your-server-ip:3000/api'

